{
  "name": "@debtmaster/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared money + split math (bigint minor units, zero float). No framework deps \u2014 usable from web, mobile, and edge functions.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
