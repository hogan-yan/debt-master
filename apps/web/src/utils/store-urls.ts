/**
 * Store listing URLs. Empty until the app is live in each store — ASO
 * metadata locks at launch (see apps/mobile/MOBILE-PLAN.md), and a dead
 * badge is worse than none. The app landing page renders an honest
 * "coming soon" state while a URL is empty and flips to real store links
 * the moment one is filled in.
 */
export const APP_STORE_URL = '';
export const GOOGLE_PLAY_URL = '';
